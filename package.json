{
  "name": "backend_repo_new",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "dev": "nodemon --exec \"npm run build && firebase emulators:start --only functions\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "firebase-functions": "^6.3.2",
    "helmet": "^8.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  }
}
